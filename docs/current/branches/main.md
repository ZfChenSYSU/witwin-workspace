# `main` 分支当前状态

更新时间：2026-08-04

## 职责

`main` 保存已经验证的跨端集成状态、公共 schema、时间与坐标协议，以及各工作分支共同依赖的稳定接口。

## 当前状态

- 当前本地与远端 `main` 指向 `3092487`，已集成 CSI Linux 基础工作流。
- 公共 session schema 已覆盖手机单端、手机 UDP、手机 + CSI 和统一数据集阶段。
- WTWN UDP 包头、坐标命名、各数据源保留原始单调时间戳等原则已经明确。
- iOS、CSI Linux 和 WiTwin 仿真的较新成果仍分别保存在工作分支；合入 `main` 前需要检查公共协议兼容性和验证证据。

## 下一步

1. 合入本次统一文档结构，使 `docs/current` 成为跨分支唯一状态入口。
2. 在公共 schema 中补齐 CSI 到手机时间轴的物化结果、时钟映射引用和适用范围。
3. 明确 P3 房间模型与 P4 统一数据集所需的网格、材料区域、外参和质量字段。
4. 各工作分支合并前运行与改动范围相匹配的测试，并同步更新本目录中的分支状态文档。

## 合入门槛

- 不破坏现有 session/UDP 主版本语义；
- 跨端字段、单位和时钟来源一致；
- 新进度有可追溯报告或机器结果支持；
- 大体积原始数据仍留在 `datasets/` 或外部存储。

